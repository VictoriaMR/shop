server {
    listen 443 ssl http2;
    server_name lmrshop.ml newsme.cf shalight.ml;
    client_max_body_size 20m;
    ssl_certificate /ssl/$host/server.crt;
    ssl_certificate_key /ssl/$host/server.key;
    ssl_protocols SSLv3 SSLv2 TLSv1 TLSv1.1 TLSv1.2;
    add_header Access-Control-Allow-Origin *;
    add_header Access-Control-Allow-Methods '*';
    access_log   on;

    location /socket.io/ {
        proxy_pass http://127.0.0.1:12008/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header X-real-ip $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
    }

    location /.well-known {
        root /www/shop/.well-known;
    }

    location /storage {
        root /www/shop/storage;
    }

    location ~* \.(js|css|ico|jpg|png|jpeg|eot|svg|ttf|woff|woff2)$ {
        if ($host = 'lmrshop.ml') {
            root /www/shop/template/admin;
            break;
        }
        root /www/shop/template/shalight;
    }

    location ~ \.php$ {
        root /www/shop;
        fastcgi_pass   127.0.0.1:9000; 
        fastcgi_index  index.php;
        fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name; 
        include        fastcgi_params;
    }

    location / {
        rewrite ^/(.*)$ /index.php?/$1 last;
        break;
    }

    location = /50x.html {
        root   html;
    }
}